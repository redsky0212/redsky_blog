import Vue from 'vue';
import Vuex from 'vuex';
import * as gnb from '@/store/modules/gnb.ts';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    gnb,
  },
});
